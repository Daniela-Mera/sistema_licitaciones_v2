<%= form_with(model: chileregion) do |form| %>
  <% if chileregion.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(chileregion.errors.count, "error") %> prohibited this chileregion from being saved:</h2>

      <ul>
        <% chileregion.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nombre_region, style: "display: block" %>
    <%= form.text_field :nombre_region %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
