<%= form_with(model: tipo_entidad) do |form| %>
  <% if tipo_entidad.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tipo_entidad.errors.count, "error") %> prohibited this tipo_entidad from being saved:</h2>

      <ul>
        <% tipo_entidad.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :entidad, style: "display: block" %>
    <%= form.text_field :entidad %>
  </div>

  <div>
    <%= form.label :descripcion, style: "display: block" %>
    <%= form.text_field :descripcion %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
