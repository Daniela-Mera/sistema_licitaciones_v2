<%= form_with(model: comuna) do |form| %>
  <% if comuna.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(comuna.errors.count, "error") %> prohibited this comuna from being saved:</h2>

      <ul>
        <% comuna.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nombre_comuna, style: "display: block" %>
    <%= form.text_field :nombre_comuna %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
